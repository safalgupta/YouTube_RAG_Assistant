# YouTube Video Q&A 🎬❓

This project is a web application that allows you to have a conversation with any YouTube video. Simply provide a YouTube URL, and the application will process its transcript and enable you to ask questions about the content. The backend is powered by Flask, LangChain, and Google's Gemini AI, with a simple, clean frontend built with HTML and Tailwind CSS.

## 🌟 Features

-   **Transcript Processing**: Automatically fetches and processes the transcript of any YouTube video.
-   **Vector Indexing**: Creates a searchable vector index of the video's content using FAISS for efficient information retrieval.
-   **Conversational Q&A**: Ask questions in natural language and receive concise, context-aware answers generated by Google's Gemini model.
-   **Simple Web Interface**: Easy-to-use interface to process videos and ask questions.
-   **Persistent Indexes**: Processed video indexes are saved locally, so you don't have to re-process them every time.

## 🛠️ Tech Stack

-   **Backend**: Python, Flask
-   **AI/ML**: LangChain, Google Generative AI (Gemini), FAISS
-   **Frontend**: HTML, Tailwind CSS, JavaScript
-   **Transcript Fetching**: [youtube-transcript-api](youtube-transcript-api-master/README.md)

## 🚀 Getting Started

Follow these instructions to set up and run the project on your local machine.

### Prerequisites

-   Python 3.8 or higher
-   A Google API Key with the "Generative Language API" enabled. You can get one from the [Google AI Studio](https://aistudio.google.com/app/apikey).

### Installation & Setup

1.  **Clone the repository:**
    ```bash
    git clone <your-repository-url>
    cd YouTube_RAG_web
    ```

2.  **Create a Python virtual environment:**
    ```bash
    python -m venv venv
    ```
    -   On Windows, activate it with:
        ```bash
        .\venv\Scripts\activate
        ```
    -   On macOS/Linux, activate it with:
        ```bash
        source venv/bin/activate
        ```

3.  **Install the dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Set up your environment variables:**
    -   Create a new file named `.env` in the root of the project.
    -   Add your Google API Key to this file:
    ````
    # .env
    GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_HERE"
    ````

### Running the Application

1.  **Start the Flask backend server:**
    ```bash
    python app.py
    ```
    The server will start running on `http://127.0.0.1:5000`.

2.  **Open the web interface:**
    -   Open the [index.html](index.html) file in your web browser.

## Usage

1.  Paste a YouTube video URL into the input field and click "Process Video".
2.  Wait for the application to fetch the transcript and build the knowledge base.
3.  Once processing is complete, the Q&A interface will appear.
4.  Type your question into the chat input and press Enter or click the send button to get an answer.

## 📂 Project Structure

```
YouTube_RAG_web/
│
├── faiss_indexes/         # Stores the generated FAISS vector indexes
│   └── ...
├── youtube-transcript-api-master/ # Local copy of the transcript library
│
├── .env                   # For storing environment variables (e.g., API keys)
├── app.py                 # Main Flask application logic
├── index.html             # Frontend user interface
└── requirements.txt       # Python package dependencies
```

## 📄 License

This project is open-source. The included `youtube-transcript-api` library is distributed under the [MIT License](youtube-transcript-api-master/LICENSE).
